# remote-sight writeup

The participant is given a WAV file, which contains an
[SSTV](https://en.wikipedia.org/wiki/Slow-scan_television) image containing the
flag.

This image can be decoded using software like `qsstv`.
